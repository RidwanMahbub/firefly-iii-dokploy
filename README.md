# firefly-iii-dokploy
A ready-to-use Docker Compose setup for deploying Firefly III on Dokploy. The official Firefly III Docker tutorial uses two separate .env files, which Dokploy doesn't support — this template merges them into a single environment variable setup that works seamlessly with Dokploy's UI.
